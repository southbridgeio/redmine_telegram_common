<%= t('telegram_common.mailer.telegram_connect.body',
      telegram_account: @telegram_account.username.present? ? "@#{@telegram_account.username}" : 'Telegram') %>
<%= link_to telegram_connect_url(user_id: @user.id, user_email: @user.mail,
                                 telegram_id: @telegram_account.telegram_id,
                                 token: @telegram_account.token,
                                 plugin: @plugin_name),
            telegram_connect_url(user_id: @user.id, user_email: @user.mail,
                                 telegram_id: @telegram_account.telegram_id,
                                 token: @telegram_account.token,
                                 plugin: @plugin_name)
%>
